import React from 'react';

import Layout from '../../components/common/Layout';

const HowItWorks = () => {
  const styles = {
    paddingTop: 150,
  };
  return (
    <Layout>
      <main style={styles}>
        <h2>How It Works</h2>
      </main>
    </Layout>
  );
};

export default HowItWorks;
