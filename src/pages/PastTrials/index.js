import React from 'react';

import Layout from '../../components/common/Layout';

const PastTrials = () => {
  const styles = {
    paddingTop: 150,
  };
  return (
    <Layout>
      <main style={styles}>
        <h2>Past Trials</h2>
      </main>
    </Layout>
  );
};

export default PastTrials;
